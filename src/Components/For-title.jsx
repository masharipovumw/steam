import React from 'react';

const ForTitle = (props) => {
    return (
        <div>
            <h3 className='main_title'>
                {props.title}
            </h3>
        </div>
    );
}

export default ForTitle;
